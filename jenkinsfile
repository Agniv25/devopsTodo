pipeline {
    agent any
    tools {
    nodejs "node"
}

    environment {
        NODEJS_HOME = tool name: 'node', type: 'NodeJSInstallation'
        PATH = "${env.NODEJS_HOME}/bin:${env.PATH}"
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Agniv25/todo-app.git'
            }
        }
        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }
        stage('Run Tests') {
            steps {
                sh 'npm test'
            }
        }
        stage('Build') {
            steps {
                sh 'npm run build'
            }
        }
        stage('Deploy') {
            steps {
                // Example deployment step (could be to a server, S3, etc.)
                // sh 'scp -r build/* user@your-server:/path/to/deploy'
                echo 'Deploying application...'
            }
        }
    }
    

    post {
        always {
            archiveArtifacts artifacts: 'build/**', allowEmptyArchive: true
            junit 'test-results.xml' // If you generate JUnit test reports
        }
        success {
            echo 'Pipeline succeeded!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}
